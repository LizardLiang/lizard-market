name: Kratos CI

on:
  push:
    branches: [ master, main, develop ]
    paths: [ 'plugins/kratos/**', 'kratos-dev/**', '.github/workflows/kratos-*.yml' ]
  pull_request:
    branches: [ master, main, develop ]
    paths: [ 'plugins/kratos/**', 'kratos-dev/**', '.github/workflows/kratos-*.yml' ]

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        go-version: ['1.25']

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ matrix.go-version }}

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Install dependencies
      working-directory: kratos-dev/go
      run: go mod download

    - name: Sync embedded assets
      shell: bash
      working-directory: kratos-dev/go
      run: |
        mkdir -p internal/cli/agents internal/cli/templates internal/cli/command-mode-suffix internal/cli/agent-protocol-slices
        cp ../../plugins/kratos/agents/*.md internal/cli/agents/
        cp ../../plugins/kratos/templates/*.md internal/cli/templates/
        cp ../../plugins/kratos/command-mode-suffix/*.md internal/cli/command-mode-suffix/
        cp ../../plugins/kratos/references/agent-protocol-slices/*.md internal/cli/agent-protocol-slices/

    - name: Run tests
      working-directory: kratos-dev/go
      shell: bash
      run: go test -v -race -coverprofile=coverage.txt -covermode=atomic ./...

    - name: Upload coverage
      if: matrix.os == 'ubuntu-latest'
      uses: codecov/codecov-action@v4
      with:
        file: ./kratos-dev/go/coverage.txt
        flags: unittests
        name: codecov-umbrella

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.25'

    - name: Sync embedded assets
      shell: bash
      working-directory: kratos-dev/go
      run: |
        mkdir -p internal/cli/agents internal/cli/templates internal/cli/command-mode-suffix internal/cli/agent-protocol-slices
        cp ../../plugins/kratos/agents/*.md internal/cli/agents/
        cp ../../plugins/kratos/templates/*.md internal/cli/templates/
        cp ../../plugins/kratos/command-mode-suffix/*.md internal/cli/command-mode-suffix/
        cp ../../plugins/kratos/references/agent-protocol-slices/*.md internal/cli/agent-protocol-slices/

    - name: golangci-lint
      uses: golangci/golangci-lint-action@v8
      with:
        version: latest
        working-directory: kratos-dev/go

  build:
    name: Build binaries
    runs-on: ubuntu-latest
    needs: [test, lint]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.25'

    - name: Sync embedded assets
      shell: bash
      working-directory: kratos-dev/go
      run: |
        mkdir -p internal/cli/agents internal/cli/templates internal/cli/command-mode-suffix internal/cli/agent-protocol-slices
        cp ../../plugins/kratos/agents/*.md internal/cli/agents/
        cp ../../plugins/kratos/templates/*.md internal/cli/templates/
        cp ../../plugins/kratos/command-mode-suffix/*.md internal/cli/command-mode-suffix/
        cp ../../plugins/kratos/references/agent-protocol-slices/*.md internal/cli/agent-protocol-slices/

    - name: Build all platforms
      working-directory: kratos-dev/go
      run: |
        GOOS=linux GOARCH=amd64 go build -o ../../plugins/kratos/bin/kratos-linux-amd64 ./cmd/kratos
        GOOS=linux GOARCH=arm64 go build -o ../../plugins/kratos/bin/kratos-linux-arm64 ./cmd/kratos
        GOOS=darwin GOARCH=amd64 go build -o ../../plugins/kratos/bin/kratos-darwin-amd64 ./cmd/kratos
        GOOS=darwin GOARCH=arm64 go build -o ../../plugins/kratos/bin/kratos-darwin-arm64 ./cmd/kratos
        GOOS=windows GOARCH=amd64 go build -o ../../plugins/kratos/bin/kratos-windows-amd64.exe ./cmd/kratos

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: kratos-binaries
        path: plugins/kratos/bin/kratos-*
        retention-days: 7
