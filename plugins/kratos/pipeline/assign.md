# assign.md (deprecated)

Deprecated in v2.111.0 — superseded by `commands/main.md` (full pipeline; spawn templates in `pipeline/stages.md`) and `commands/quick.md` (simple tasks), both delegating via `Task(subagent_type: "kratos:<agent>")`; kept so old links resolve.
